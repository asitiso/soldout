# 생존약국 — 매장 확장과 배치 편집

## 목적
기존 시설/재고를 다시 만들지 않고 매장을 확장하고, 같은 화면에서 매대를 옮겨 동선을 개선한다. 잘못된 배치로 게임이 멈추지 않도록 미리 검증한다.

## 이번 범위
- 3단계 매장: 8평/12평/16평. 오른쪽 이웃 공간으로 연결되며 기존 공간과 출입구를 유지한다.
- 기존 상품 매대 3개를 이동/90도 회전. 상품과 가격표가 함께 이동하며 검색/재진열 목적지도 갱신한다.
- 화분/대기 벤치/창고 선반을 설치·이동·회전·판매. 창고 선반은 실질적으로 보관 한도를 늘린다.
- 확장에 따른 창고 한도, 직원 한도, 임대료를 명시한다. 기존 재고/고객/진행 업무는 보존한다.
- 편집 모드: 자동 일시정지, 실제 크기 고스트, 가능/불가 미리보기, R 회전, Esc 배치 취소, 편집 종료 시 원래 속도 복원.
- 저장 버전 2. v1 저장의 매대 위치/기존 화분을 이전한다. 불량 배치/잘못된 확장 단계는 거부한다.

## 구현 단위
1. src/building.js: 확장/가구 정의, 회전 좌표, 상태 기반 장애물, 연결성 검사. tests/building.test.js에 기존 재고 보존·수납 초과·막힌 배치·저장 이전 테스트를 먼저 작성.
2. src/simulation.js / save-validation.js: 확장/편집 트랜잭션, 비용, 기존 업무 재경로, 저장 이전. 전체 기존 테스트 유지.
3. src/world.js / models.js: 실제 공간 확장, 매대 그룹 이동, 가구 모델, 가변 경계 카메라, 고스트.
4. src/ui.js / main.js / style.css: 통합 매장 패널, 편집 툴바, 회전/취소, 컨텍스트 조작. 브라우저에서 실제 클릭으로 검증.

## 검증 포인트
주문/고객 보유/매대 재고 보존. 이동 후 고객이 옛 위치에서 구매하지 않음. 기존 진행 업무가 도착 가능한 새 목적지를 사용. 막힌 출입구/대기열/조제 공간 거부. 확장 후 저장 복원. 미리보기만으로 현금/레이아웃이 바뀌지 않음. 취소와 반복 편집이 게임 속도를 잃지 않음.

## 후속 한계
카운터·조제실·출입문 재배치는 이번에 포함하지 않는다. 전체 벽/방 구조 편집과 다중 서비스 카운터는 후속이며, 기존 3개 매대와 구매 가구를 편집 대상으로 명확히 표시한다.

## 실행 결과

2026-09-21: 위 구현 단위 완료. 자동 테스트 27개 및 Edge 편집/영업 흐름 통과. 독립 검토의 2개 중요 문제를 회귀 테스트로 수정했다. 저장 이전 시 충돌 화분은 가까운 빈자리로 이동하며, 불가능하면 전액 환불한다. 읽기 실패 원본은 자동 저장으로 덮어쓰지 않는다. 자세한 증거와 남은 범위는 MILESTONES.md에 기록했다.
