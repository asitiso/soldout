# Architecture

- src/content.js: 상품·공급업체·처방·POP·뉴스 등 정적 콘텐츠.
- src/crisis.js / crisis-ui.js: 10단계 경제/방문 곡선과 현재 현황·내일 예보. 발주 잔여량은 orders의 orderedDay로 계산한다.
- src/crowd.js: 매장별 입장 한도와 외부 대기 좌표. 시뮬레이션의 outside→enter 전이는 선착순이며 enter도 실내 예약으로 계산한다.
- src/simulation.js: 직렬화 가능한 상태, 고정 시간 업데이트, 고객 상태기계, 직원 업무 단계, 경제와 재고.
- src/navigation.js: 0.25m 경로 탐색, 0.5m 배치 격자. 가구 충돌 회피 및 배치 시 연결성 검사.
- src/movement.js: 이동 직전 사람 간 거리 검사, 사람을 임시 장애물로 포함한 재경로, 막힌 목적지 근처 비켜서기. 0.35초 재시도 간격은 WeakMap에만 보관하며 저장하지 않는다. 직원 대기/고객 생성 위치도 선택한다.
- src/save-validation.js: 저장 파일의 구조·역할·업무 연결·재고 보존 검증. 상태 교체 전에 실행.
- src/building.js: 확장/가구 정의, 가변 장애물·경계, 대기줄과 사람의 도달 가능성 검증, 이전 저장 마이그레이션.
- src/building-ui.js: 매장 패널과 편집 모드. 편집 중 일시정지하고 종료 시 원래 속도를 복원.
- src/world.js: Three.js 렌더링, 카메라, 월드 선택, 조명, 모델 동기화.
- src/models.js: 다중 파트 캐릭터 및 소품 모델. 런타임 제작한 자체 에셋.
- src/render-batching.js: 캐릭터의 관절별 정적 부품을 vertex-color mesh로 병합. 움직이는 그룹·숨긴 소품·텍스처는 분리 유지한다.
- src/browsing.js: 매대 대기 위치를 가구·서비스 대기열·기존 매대 대기자와 겹치지 않는 도달 가능한 격자에서 선택한다. 자리가 없으면 현재 위치에서 기다린다. 시뮬레이션의 browse/pick 상태가 매대 예약이다.
- src/ui.js: HUD·컨텍스트·업무·패널·정산. 시뮬레이션 메서드로만 상태 변경.
- src/audio.js / audio-score.js: 사용자 상호작용으로 활성화되는 Web Audio 믹서, 독립 효과음/배경음 버스, 합성 악보, 오디오 재개 재시도, 노드 정리 및 탭 숨김 처리.
- src/audio-ui.js / audio.css: 전체/효과음/배경음 음량, 음소거, 소리 테스트와 실제 출력 레벨. 오디오 설정은 게임 저장과 분리된 sold-out-audio-v1 슬롯에 보존한다.
- src/main.js: 조립, 스텝 루프, 저장 경계, 키보드.

화면은 상태의 투영이다. 재고 변경 시 실제 매대 모델을 동기화한다. 이벤트 큐는 음향/알림 연출에 사용하며 저장하지 않는다. 작업은 단계별 목적 좌표/행동/기간을 가지며 이동 완료 이후에만 진행한다. 시뮬레이션을 렌더러와 분리하여 50명 부하 테스트가 가능하다.
