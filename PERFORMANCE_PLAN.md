# 군중 표현과 매대 접근 Implementation Plan

> 직접 실행: superpowers:executing-plans, 마지막 독립 검토.

**Goal:** 같은 외형과 관절 애니메이션을 유지하면서 50명 렌더링 작업을 줄이고, 여러 손님이 한 지점에서 상품을 집는 현상을 막는다.
**Architecture:** 정적인 캐릭터 부품을 관절 그룹별 vertex-color mesh로 병합한다. 매대 접근은 zone별 한 명의 browsing/picking 예약으로 관리하고 나머지는 고유 대기 위치에서 기다린다.
**Spec:** MASTER_DIRECTIVE.md의 캐릭터·군중·성능. 모든 사람의 물리 충돌 처리는 범위 밖이며 매대 사용 중첩부터 해소한다.

## 선택
- 복잡한 물리 엔진과 모델 단순화 대신 기존 모델의 색·기하·관절을 유지하는 병합을 선택한다.
- 동일 매대 예약은 저장 상태의 browse/pick으로 계산한다. 별도 저장 장부를 만들지 않는다.
- 매대 대기는 기존 outside처럼 인내심과 마감 처리를 받는다. 대기 위치는 각 매대 앞의 도달 가능한 빈 격자이며 가구 편집 시 재계산한다.

## 작업
- [x] render-batching.test.js: 변환된 위치/색/관절/숨김 메시 유지와 draw mesh 감소 테스트를 먼저 실패시킨다.
- [x] src/render-batching.js, models.js: 관절 직속 정적 Mesh만 병합. 숨김 상품·그림자·텍스처는 제외. 폐기 시 병합 전 기하도 정리한다.
- [x] browsing.test.js: 동일 매대 동시 집기 금지, 차례대로 판매, 재배치·저장·마감 테스트 작성 후 shelfwait 상태/위치 예약 구현.
- [x] 브라우저 50명 전후 프레임 측정 및 화면 확인. 전체 테스트/빌드/독립 검토.

## Review Focus
공유 재질 폐기, 회전/스케일 보존, 숨김 소품 노출, 매대 이동 중 대기자, 한 매대 대기 때문에 다른 매대 손님이 멈추는 문제를 검증한다.

## 결과
자동 테스트 44개, 매대 이용 및 50명 Edge 브라우저 검증, 프로덕션 빌드 통과. 독립 검토의 이전 저장 다중 이용 문제를 회귀 테스트로 수정했다. 로컬 프레임 샘플 약 42→59fps. 모든 보행자의 물리적 충돌 회피는 범위 밖으로 유지한다.
