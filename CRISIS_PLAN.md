# 생존약국 위기 운영 Implementation Plan

> 실행: superpowers:executing-plans. 기존 사용자의 연속 구현 지시에 따라 직접 구현하고 마지막 독립 검토를 수행한다.

**Goal:** 위기의 상승과 회복이 실제 발주·방문량에 반영되고, 플레이어가 다음 날을 준비할 수 있게 한다.
**Architecture:** crisis.js의 10단계 정의를 시뮬레이션과 뉴스 패널이 공유한다. 일일 공급량은 주문의 orderedDay에서 계산하여 별도 중복 장부를 만들지 않는다.
**Spec:** MASTER_DIRECTIVE.md의 위기/수요/공급 루프. 이번에는 기존 18명 입장 한도를 유지하고 과부하 시 운영 종료를 검증한다. 50명 충돌 회피와 외부 대기는 별도 범위다.

## 선택과 제약
- 날짜별 고정 곡선을 사용한다. 무작위 뉴스로 숨은 패널티를 주는 방식보다 다음 날 준비가 쉽고 재현 가능한 검증이 가능하다.
- DAY 1–3 평시, DAY 4–12 단계 1–9. 정점 이후 매입가·배송·수요 모두 회복한다.
- 기존 v2 저장을 유지한다. orderedDay가 없는 이전 주문은 로드한 당일 주문으로 보수적으로 이전한다. 재로드로 한도를 늘릴 수 없다.
- 원래 재고/현금/배송 계약은 유지한다. 이미 발주한 가격과 ETA를 다음 날 다시 계산하지 않는다.

## 작업과 검증
- [x] tests/crisis.test.js: 회복기 공급 정상화, 하루 중 분할 발주 한도, 다음 날 한도 회복, 공급업체 분리, 이전 저장 이전과 손상 일자 거절을 실패 상태로 확인.
- [x] src/crisis.js: CRISIS_STAGES 및 stageForDay(day)를 구현. 단계별 demand/traffic/cost/delivery/supply와 설명/행동 안내 정의.
- [x] src/simulation.js: demand/quote/방문 간격이 공통 단계를 사용. 주문 orderedDay 기록. quote.limit는 당일 남은 수량이며 dailyLimit도 반환.
- [x] src/save-validation.js와 building.js: orderedDay 정수 및 현재 일자 이하 검증, 누락 필드만 이전.
- [x] src/crisis-ui.js와 ui.js: 현재 단계/수요/매입/배송/내일 예고와 발주·재고·직원 바로가기. 발주 기본 수량은 남은 공급량 이하, 매진 시 비활성화.
- [x] 18명 동시 입장 후 마감 가능/재고 보존을 테스트. 실제 Edge에서 위기 뉴스와 한도 소진/저장 복원을 검증하고 빌드.

## Review Focus
같은 상품 분할 발주, 다른 공급업체 주문, 이전 저장의 당일 물량, 회복기 공급, 이미 이동 중인 배송의 날짜 변경을 각각 자동 테스트로 검증한다.

## 결과
자동 테스트 34개, 위기 브라우저 흐름, 프로덕션 빌드 통과. 12일 운영과 매일 저장 복원 검증. 독립 검토의 회복 후 POP 문제를 실패 테스트로 재현 후 수정. Three.js 청크 크기 경고는 기존과 동일하며 빌드는 성공한다.
